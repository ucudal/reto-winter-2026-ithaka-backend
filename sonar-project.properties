# Identificador ÚNICO del proyecto en el SonarQube compartido (coordinar con los otros grupos
# y el admin para que no colisione).
sonar.projectKey=ithaka-backend
sonar.projectName=Ithaka Backend

# Código a analizar
sonar.sources=app
sonar.python.version=3.13

# Excluir lo que no es código propio
sonar.exclusions=**/__pycache__/**,alembic/versions/**

# Cobertura (opcional; descomentar cuando haya tests):
# sonar.python.coverage.reportPaths=coverage.xml
